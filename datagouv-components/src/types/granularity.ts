export type Granularity = {
  id: string
  name: string
}

export type Granularities = Array<Granularity>

export type SpatialZone = {
  code: string
  id: string
  level: string
  name: string
  uri: string
}
